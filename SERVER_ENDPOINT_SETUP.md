# 🖥️ Настройка серверного endpoint для деплоя

Инструкция по созданию endpoint на сервере для приема запросов от пакета `letoceiling-coder/deploy`.

## 📍 Где создавать endpoint

Endpoint нужно создать **на сервере**, где развернут ваш Laravel проект (в данном случае на `letocewh.beget.tech`).

## 🚀 Быстрая установка

### Шаг 1: Подключитесь к серверу

```bash
ssh letocewh@ПРАВИЛЬНЫЙ_ХОСТ
cd ~/letocewh.beget.tech/public_html
```

### Шаг 2: Откройте файл routes/api.php

```bash
nano routes/api.php
```

### Шаг 3: Добавьте endpoint

Скопируйте код из файла `routes/api.php.example` в ваш `routes/api.php`.

Или добавьте в конец файла:

```php
Route::post('/deploy', function (Request $request) {
    // Код из routes/api.php.example
})->middleware('api');
```

### Шаг 4: Проверьте .env на сервере

Убедитесь, что на сервере в `.env` есть:

```env
DEPLOY_TOKEN=123123123
COMPOSER_PATH=/home/l/letocewh/bin/composer
```

### Шаг 5: Проверьте работу

```bash
# Тест endpoint локально на сервере
curl -X POST http://localhost/api/deploy \
  -H "Authorization: Bearer 123123123" \
  -H "Content-Type: application/json" \
  -d '{"branch":"main","version":"test","with_seed":false}'
```

## 📝 Полный код endpoint

См. файл `routes/api.php.example` для полного кода.

## ⚙️ Настройка для Beget

Учитывая особенности Beget, добавьте в `.env` на сервере:

```env
# Путь к composer (если используете wrapper)
COMPOSER_PATH=/home/l/letocewh/bin/composer

# Путь к PHP (если нужно)
PHP_PATH=/usr/local/bin/php8.2
```

## 🔒 Безопасность

1. **Токен:** Используйте сложный токен в `.env`
2. **HTTPS:** Используйте HTTPS для endpoint
3. **IP Whitelist:** (Опционально) Ограничьте доступ по IP

## ✅ Проверка работы

После настройки endpoint, протестируйте с локальной машины:

```bash
# С локальной машины (где установлен пакет)
php artisan deploy --dry-run

# Реальный деплой
php artisan deploy --message="Test deployment"
```

## 📚 Дополнительная информация

- [DEPLOY_SERVER_EXAMPLE.md](DEPLOY_SERVER_EXAMPLE.md) - Подробный пример с объяснениями
- [README.md](README.md) - Общая информация о пакете

