# 📦 Установка Laravel Deploy Package

Подробное руководство по установке пакета `letoceiling-coder/deploy` в Laravel проект.

## Требования

- **PHP:** 8.1 или выше
- **Laravel:** 10.0+ или 11.0+
- **Git:** установлен и настроен
- **Node.js и npm:** (опционально, для сборки фронтенда)
- **Composer:** последняя версия

## Способ 1: Установка через Composer (рекомендуется)

### Шаг 1: Установка пакета

```bash
composer require letoceiling-coder/deploy
```

### Шаг 2: Автоматическая регистрация

Пакет автоматически регистрируется через Laravel Package Auto-Discovery. Никаких дополнительных действий не требуется!

### Шаг 3: Проверка установки

Проверьте, что команда доступна:

```bash
php artisan list | grep deploy
```

Должна появиться команда `deploy`.

## Способ 2: Установка из GitHub (для разработки)

### Шаг 1: Добавление репозитория в composer.json

```json
{
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/letoceiling-coder/deploy.git"
        }
    ],
    "require": {
        "letoceiling-coder/deploy": "dev-main"
    }
}
```

### Шаг 2: Установка

```bash
composer update
```

## Способ 3: Ручная установка (если auto-discovery не работает)

### Шаг 1: Установка пакета

```bash
composer require letoceiling-coder/deploy
```

### Шаг 2: Регистрация Service Provider

Откройте `config/app.php` и добавьте в массив `providers`:

```php
'providers' => [
    // ...
    LetoceilingCoder\Deploy\DeployServiceProvider::class,
],
```

## Настройка переменных окружения

### Шаг 1: Откройте файл `.env`

```bash
nano .env
```

### Шаг 2: Добавьте обязательные переменные

```env
# Git репозиторий (куда будет отправляться код)
GIT_REPOSITORY_URL=https://github.com/your-username/your-repo.git

# URL сервера для деплоя (endpoint, который принимает запросы)
DEPLOY_SERVER_URL=https://your-server.com/api/deploy

# Токен авторизации (для защиты endpoint)
DEPLOY_TOKEN=your-secret-token-here-min-32-chars
```

### Шаг 3: Опциональные переменные

```env
# Таймаут для HTTP запросов (секунды)
DEPLOY_TIMEOUT=300

# Таймаут для npm команд (секунды)
NPM_TIMEOUT=600

# Автоматическая установка npm пакетов перед сборкой
DEPLOY_AUTO_NPM_INSTALL=true

# Включить логирование
DEPLOY_LOGGING=true
```

## Публикация конфигурации (опционально)

Если вы хотите изменить настройки пакета, опубликуйте конфигурационный файл:

```bash
php artisan vendor:publish --tag=deploy-config
```

Это создаст файл `config/deploy.php`, который вы можете редактировать.

## Проверка установки

### 1. Проверка команды

```bash
php artisan deploy --help
```

### 2. Проверка конфигурации (dry-run)

```bash
php artisan deploy --dry-run
```

Это покажет все шаги без выполнения.

### 3. Проверка окружения

Убедитесь, что все инструменты доступны:

```bash
# Git
git --version

# Composer
composer --version

# npm (если используется)
npm --version
```

## Настройка Git (если еще не настроен)

### Шаг 1: Инициализация репозитория

```bash
git init
```

### Шаг 2: Добавление remote

```bash
git remote add origin https://github.com/your-username/your-repo.git
```

### Шаг 3: Настройка пользователя (если нужно)

```bash
git config user.name "Your Name"
git config user.email "your.email@example.com"
```

## Настройка сервера

Сервер должен иметь endpoint, который принимает POST запросы. Подробнее см. [DEPLOY_SERVER_EXAMPLE.md](DEPLOY_SERVER_EXAMPLE.md).

## Быстрая проверка

После установки выполните:

```bash
php artisan deploy --dry-run --message="Test deployment"
```

Если все настроено правильно, вы увидите список шагов, которые будут выполнены.

## Решение проблем

### Ошибка: "Class not found"

**Решение:** Очистите кэш:

```bash
php artisan clear-compiled
php artisan config:clear
composer dump-autoload
```

### Ошибка: "Command not found"

**Решение:** Убедитесь, что Service Provider зарегистрирован:

```bash
php artisan package:discover
```

### Ошибка: "DEPLOY_TOKEN is not set"

**Решение:** Добавьте переменную в `.env` файл и перезапустите приложение.

## Следующие шаги

После успешной установки:

1. Прочитайте [USAGE.md](USAGE.md) для примеров использования
2. Настройте серверный endpoint (см. [DEPLOY_SERVER_EXAMPLE.md](DEPLOY_SERVER_EXAMPLE.md))
3. Выполните первый деплой: `php artisan deploy`

## Дополнительная информация

- [README.md](README.md) - Общая информация о пакете
- [USAGE.md](USAGE.md) - Примеры использования
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем
- [DEPLOY_SERVER_EXAMPLE.md](DEPLOY_SERVER_EXAMPLE.md) - Пример серверного endpoint

