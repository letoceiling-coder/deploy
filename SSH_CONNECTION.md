# 🔌 Подключение к серверу Beget по SSH

Инструкция по подключению к серверу Beget для тестирования пакета.

## ⚠️ Важно: Правильный хост для Beget

**`server.beget.com` - это НЕ правильный хост!**

Для Beget используется один из следующих вариантов:

## Вариант 1: Через панель управления Beget (РЕКОМЕНДУЕТСЯ)

1. Войдите в панель управления Beget: https://beget.com/ru
2. Перейдите в раздел **Хостинг → SSH**
3. Включите SSH (если еще не включен)
4. **Скопируйте правильный хост** из панели - он будет указан там

Обычно формат такой:
```
ssh login@serverXX.beget.tech
```
или
```
ssh login@hostXX.beget.pro
```

Где `XX` - номер сервера, `login` - ваш логин.

## Вариант 2: Использование IP адреса

Если знаете IP адрес сервера:

```bash
ssh letocewh@IP_АДРЕС
```

IP адрес можно узнать:
- В панели Beget (Хостинг → SSH)
- Через команду `ping letocewh.beget.tech` (покажет IP домена)

## Вариант 3: Через домен (если настроен)

Попробуйте подключиться через ваш домен:

```bash
ssh letocewh@letocewh.beget.tech
```

## Вариант 4: Указание порта

Если SSH работает на нестандартном порту:

```bash
ssh -p ПОРТ letocewh@ХОСТ
```

## 🔍 Как найти правильный хост

### Способ 1: В панели Beget

1. Войдите в панель: https://beget.com/ru
2. **Хостинг → SSH → Включить SSH**
3. Там будет указан **точный хост для подключения**

Пример:
```
ssh letocewh@server12.beget.tech
```

### Способ 2: Через ping

```bash
ping letocewh.beget.tech
```

Это покажет IP адрес, но не хост для SSH.

### Способ 3: Через веб-консоль Beget

Если SSH недоступен, используйте веб-консоль в панели Beget для выполнения команд.

## 📝 Правильная команда подключения

После того, как узнаете правильный хост из панели:

```bash
ssh letocewh@ПРАВИЛЬНЫЙ_ХОСТ
```

Например:
```bash
ssh letocewh@server12.beget.tech
```

## 🔐 Первое подключение

При первом подключении SSH спросит:

```
The authenticity of host 'server12.beget.tech' can't be established.
Are you sure you want to continue connecting (yes/no)?
```

Введите `yes` и нажмите Enter.

## 🚨 Решение проблем

### Ошибка: "Could not resolve hostname"

**Причина:** Неправильный хост или DNS проблема.

**Решение:**
1. Проверьте хост в панели Beget
2. Используйте IP адрес вместо хоста
3. Проверьте интернет-соединение

### Ошибка: "Connection refused"

**Причина:** SSH не включен или порт закрыт.

**Решение:**
1. Включите SSH в панели Beget: **Хостинг → SSH → Включить SSH**
2. Подождите 5-10 минут после включения
3. Попробуйте подключиться снова

### Ошибка: "Permission denied"

**Причина:** Неправильный логин или пароль.

**Решение:**
1. Проверьте логин (обычно это ваш логин в панели Beget)
2. Используйте правильный пароль
3. Рассмотрите использование SSH ключей

## 🔑 Настройка SSH ключей (опционально)

Для удобства можно настроить SSH ключи:

### 1. Генерация ключа (на локальной машине)

```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

### 2. Копирование ключа на сервер

```bash
ssh-copy-id letocewh@ПРАВИЛЬНЫЙ_ХОСТ
```

Или вручную:
```bash
cat ~/.ssh/id_rsa.pub | ssh letocewh@ПРАВИЛЬНЫЙ_ХОСТ "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

## 📋 Быстрая проверка

После подключения проверьте:

```bash
# Текущая директория
pwd

# Версия PHP
php -v

# Версия Composer
composer -V

# Версия Node.js
node -v

# Версия npm
npm -v

# Git статус
git status
```

## 🎯 Следующие шаги после подключения

1. Перейдите в директорию проекта:
```bash
cd ~/www/letocewh.beget.tech
# или
cd /path/to/your/project
```

2. Установите пакет:
```bash
composer require letoceiling-coder/deploy
```

3. Запустите тесты:
```bash
php tests/TestEnvironment.php
```

## 💡 Альтернатива: Локальное тестирование

Если SSH недоступен, можно тестировать пакет локально:

1. Установите пакет локально
2. Настройте .env с тестовыми данными
3. Используйте `--dry-run` для проверки
4. Настройте тестовый endpoint локально

## 📞 Поддержка Beget

Если не можете найти правильный хост:

1. Обратитесь в поддержку Beget
2. Укажите ваш логин: `letocewh`
3. Попросите предоставить правильный хост для SSH подключения

---

**Важно:** Всегда используйте хост, указанный в панели управления Beget!

